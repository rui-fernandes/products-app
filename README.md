# Products App
This solution is a demo app for learning purposes.

## Technologies
* Node.js
* mongodb

## How to use
* run `npm install`
* run `npm start` to start the server (keep the terminal window open)
* run `npm run start:server` to start the Node.js server